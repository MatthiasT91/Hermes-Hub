services:
  hermes-hub:
    build: .
    container_name: hermes-hub
    ports:
      - "8063:8063"
    restart: always
    env_file:
      - .env
    environment:
      - PORT=8063
    volumes:
      - .:/app
      - /app/node_modules
