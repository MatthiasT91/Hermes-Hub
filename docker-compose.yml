services:
  hermes-hub:
    build: .
    container_name: hermes-hub
    ports:
      - "${PORT:-8063}:${PORT:-8063}"
    restart: always
    volumes:
      - ./network_state.json:/app/network_state.json
    env_file:
      - .env