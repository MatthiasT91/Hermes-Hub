services:
  hermes-hub:
    build: .
    container_name: hermes-hub
    ports:
      - "${PORT:-8080}:${PORT:-8080}"
    restart: always
    volumes:
      - ./network_state.json:/app/network_state.json
    env_file:
      - .env