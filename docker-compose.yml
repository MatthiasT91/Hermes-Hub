services:
  hermes-hub:
    build: .
    container_name: hermes-hub
    ports:
      - "8080:8080"
    restart: always
    env_file:
      - .env
    volumes:
      - .:/app
      - /app/node_modules
